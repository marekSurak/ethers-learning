import { styled } from 'styles/stitches.config'

export const Form = styled('form', {
  display: 'flex',
  gap: '$2',
})

export const TxHistory = styled('div', {
  marginTop: '$3',
})

export const TxLine = styled('div', {
  display: 'flex',
  width: '100%',
  justifyContent: 'space-between',
  alignItems: 'center',
  marginTop: '$1',
})
